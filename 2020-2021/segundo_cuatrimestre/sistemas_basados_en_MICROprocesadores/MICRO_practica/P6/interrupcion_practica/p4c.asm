CODIGO SEGMENT
	ASSUME CS:CODIGO

	ORG 256

	INICIO: JMP INSTALADOR
	
	TABLA DB "ABCDF"
	FLAG DW 0
	
	RSI PROC FAR
		IRET
	RSI ENDP
	INSTALADOR PROC
		MOV AX, 0
		MOV ES, AX
		MOV AX, OFFSET RSI
		MOV BX, CS
		CLI
		MOV ES:[55H*4], AX
		MOV ES:[55H*4+2], BX
		STI
		MOV DX, OFFSET INSTALADOR
		INT 27H
	INSTALADOR ENDP

CODIGO ENDS
END INICIO